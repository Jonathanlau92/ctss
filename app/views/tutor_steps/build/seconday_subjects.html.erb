<h1 class="mt-3"> Subject preferences (Secondary level) </h1>
<p>Please check all the subjects that you are able to teach, so that we can be more flexible in arranging tutor-tutee matches.</p>
<%= form_for @tutor, url: wizard_path do |f|%>
  <% @tutor.secondary_level.each do |subject| %>
    <%= check_box_tag "secondary_subject_ids[]", subject %>
    <%= label_tag subject %>
    <br>
  <% end %>
  <%= f.submit "Next", class: 'btn btn-sm btn-primary'%>
<%end%>

