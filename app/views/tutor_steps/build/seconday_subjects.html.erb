  <div class="progress mt-3">
  <div class="progress-bar" role="progressbar" style="width:48%;" aria-valuenow="20" aria-valuemin="0" aria-valuemax="100"></div>
</div>
  <h1 class="mt-3 text-center">
  <span>Subject preferences</span>
  <span>(Secondary level)</span>
  </h1>
  <div class="ml-3 mr-3">
    <h2 class="mt-3 mb-3">Please check all the subjects that you are able to teach, so that we can be more flexible in arranging tutor-tutee matches.</h2>
    <%= form_for @tutor, url: wizard_path, method: :put do |f|%>
      <% @tutor.secondary_level.each do |subject| %>
        <%= check_box_tag "secondary_subject_ids[]", subject %>
        <%= label_tag subject %>
      <br>
    <% end %>
      <div class='d-flex justify-content-center'>
        <%= link_to 'Back', wizard_path(:primary_subjects), class: 'btn back-button' %>
        <%= f.submit 'Next', class: 'btn next-button' %>
      </div>
    <%end%>
  </div>
