<div class='text-center'>Add progress bar here</div>
  <h1 class='mt-3'>Help us to get to know you better</h1>
  <div class='form-group'>
  <%= form_for @tutor do |f| %>
    <div class='form-group'>
      <%= f.label :full_name %>
      <%= f.text_field :full_name, class: 'form-control' %>
    </div>
    <div class='form-group'>
      <%= f.label :school_email %>
      <%= f.text_field :school_email, class: 'form-control' %>
    </div>
    <div class='form-group'>
      <%= f.label :alternate_email %>
      <span>A personal email is preferred here, just in case our emails sent to official school emails are blocked.</span>
      <%= f.text_field :alternate_email, class: 'form-control' %>
    </div>
    <div class='form-group'>
      <%= f.label :sex %>
      <div class='ml-5 form-check form-check-inline'>
        <%= f.radio_button :sex, 0, class:'mr-3 mb-2 form-check-label' %> 
        <%= f.label :sex, 'Male', class: 'form-check-input' %>
      </div>
      <div class='ml-5 form-check form-check-inline'>
        <%= f.radio_button :sex, 1, class:'mr-3 mb-2 form-check-label' %>
        <%= f.label :sex, 'Female', class: 'form-check-input' %>
      </div>
    </div>
    <div class='form-group'>
      <%= f.label :hours_to_teach, 'How many hours can you commit per week?' %>
      <span>For each student, it is recommended that you conduct 1-2 hours of online tutoring weekly, preferably on the weekends.</span>
      <%= f.number_field :hours_to_teach, class: 'form-control' %>
    </div>
    <div class='d-flex justify-content-center'>
      <%= link_to 'Back', tutor_welcome_path, class: 'btn back-button' %>
      <%= f.submit 'Next', class: 'btn next-button' %>
    </div>
  <% end %>
  </div>
