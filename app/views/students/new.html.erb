  <%= image_tag 'student-progress1.png', class:'mt-4 mx-auto d-block' %> 
  <h1 class='mt-3 text-center'>Help us to get to know you better</h1>
  <div class='form-group'>
  <%= form_for @student do |f| %>
    <div class='form-group'>
      <%= f.label :full_name %>
      <%= f.text_field :full_name, class: 'form-control' %>
    </div>
    <div class='form-group'>
      <%= f.label :school_email %>
      <span>For Verification</span>
      <%= f.email_field :school_email, class: 'form-control' %>
    </div>
    <div class='form-group'>
      <%= f.label :alternate_email %>
      <span>Parent email is preferred here, just in case our emails sent to official school emails are blocked.</span>
      <%= f.email_field :alternate_email, class: 'form-control' %>
    </div>
    <div class='form-group'>
      <%= f.label :sex %>
      <div class='ml-5 form-check form-check-inline'>
        <%= f.radio_button :sex, 0, class:'mr-3 mb-2 form-check-label' %> 
        <%= f.label :sex, 'Male', class: 'form-check-input' %>
      </div>
      <div class='ml-5 form-check form-check-inline'>
        <%= f.radio_button :sex, 1, class:'mr-3 mb-2 form-check-label' %>
        <%= f.label :sex, 'Female', class: 'form-check-input' %>
      </div>
    </div>
    <div class='form-group'>
      <%= f.label :contact_number %>
      <span>Your contact number will only be accessible by the CTSS Team and no one else. It will only be used it the extreme case when the tutor is unable to contact you via email for tutoring. This number will be a way for us to contact you immediately, and will not be sent to anyone.</span>
      <%= f.number_field :contact_number, class: 'form-control' %>
    </div>
    <%  @education={'PSLE'=>'Primary School', 'NT_level'=>'Secondary N(T)-Level', 'NA_level'=>'Secondary N(A)-Level', 'O_level'=>'Secondary O-Level', 'A_level'=>'Junior College A-Level'} %>
    <div class='form-group'>
      <%= f.label :education_level %>
      <%= f.select :education_level, options_for_select(@education.map {|key, value| [value, key]}), {include_blank: true}, class: 'form-control' %>
    </div>
    <div class='d-flex justify-content-center'>
      <%= link_to 'Back', student_welcome_path, class: 'btn back-button' %>
      <%= f.submit 'Next', class: 'btn next-button' %>
    </div>
  </div>
  <% end %>